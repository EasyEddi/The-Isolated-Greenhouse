name: Build Game Releases

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

env:
  PROJECT_PATH: game/UnrealProject/TheIsolatedGreenhouse.uproject
  WINDOWS_UE_ROOT: C:\Program Files\Epic Games\UE_5.8
  MAC_UE_ROOT: /Users/Shared/Epic Games/UE_5.8

jobs:
  build-windows:
    name: Build Windows release
    runs-on: [self-hosted, Windows]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Pull Git LFS files
        shell: pwsh
        run: git lfs pull

      - name: Verify Unreal Engine
        shell: pwsh
        run: |
          $RunUat = Join-Path "${{ env.WINDOWS_UE_ROOT }}" "Engine\Build\BatchFiles\RunUAT.bat"
          if (!(Test-Path $RunUat)) {
            Write-Error "RunUAT.bat not found at $RunUat. Install Unreal Engine 5.8 on this runner or update WINDOWS_UE_ROOT in this workflow."
          }

      - name: Build packaged Windows game
        shell: pwsh
        run: |
          $Project = Join-Path "$env:GITHUB_WORKSPACE" "${{ env.PROJECT_PATH }}"
          $Archive = Join-Path "$env:GITHUB_WORKSPACE" "dist\windows"
          $RunUat = Join-Path "${{ env.WINDOWS_UE_ROOT }}" "Engine\Build\BatchFiles\RunUAT.bat"

          & $RunUat BuildCookRun `
            -project="$Project" `
            -noP4 `
            -platform=Win64 `
            -clientconfig=Shipping `
            -serverconfig=Shipping `
            -cook `
            -allmaps `
            -build `
            -stage `
            -pak `
            -prereqs `
            -archive `
            -archivedirectory="$Archive"

      - name: Zip Windows release
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "$env:GITHUB_WORKSPACE\release" | Out-Null
          Compress-Archive `
            -Path "$env:GITHUB_WORKSPACE\dist\windows\*" `
            -DestinationPath "$env:GITHUB_WORKSPACE\release\IsolatedGreenhouse-Windows.zip" `
            -Force

      - name: Upload Windows release artifact
        uses: actions/upload-artifact@v4
        with:
          name: IsolatedGreenhouse-Windows
          path: release/IsolatedGreenhouse-Windows.zip
          if-no-files-found: error

  build-macos:
    name: Build macOS release
    runs-on: [self-hosted, macOS]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Pull Git LFS files
        shell: bash
        run: git lfs pull

      - name: Verify Unreal Engine
        shell: bash
        run: |
          RUN_UAT="${{ env.MAC_UE_ROOT }}/Engine/Build/BatchFiles/RunUAT.sh"
          if [ ! -f "$RUN_UAT" ]; then
            echo "RunUAT.sh not found at $RUN_UAT. Install Unreal Engine 5.8 on this runner or update MAC_UE_ROOT in this workflow." >&2
            exit 1
          fi

      - name: Build packaged macOS game
        shell: bash
        run: |
          PROJECT="$GITHUB_WORKSPACE/${{ env.PROJECT_PATH }}"
          ARCHIVE="$GITHUB_WORKSPACE/dist/mac"
          RUN_UAT="${{ env.MAC_UE_ROOT }}/Engine/Build/BatchFiles/RunUAT.sh"

          "$RUN_UAT" BuildCookRun \
            -project="$PROJECT" \
            -noP4 \
            -platform=Mac \
            -clientconfig=Shipping \
            -serverconfig=Shipping \
            -cook \
            -allmaps \
            -build \
            -stage \
            -pak \
            -archive \
            -archivedirectory="$ARCHIVE"

      - name: Zip macOS release
        shell: bash
        run: |
          mkdir -p "$GITHUB_WORKSPACE/release"
          cd "$GITHUB_WORKSPACE/dist"
          ditto -c -k --sequesterRsrc --keepParent mac "$GITHUB_WORKSPACE/release/IsolatedGreenhouse-macOS.zip"

      - name: Upload macOS release artifact
        uses: actions/upload-artifact@v4
        with:
          name: IsolatedGreenhouse-macOS
          path: release/IsolatedGreenhouse-macOS.zip
          if-no-files-found: error

  publish-release:
    name: Publish GitHub release
    needs:
      - build-windows
      - build-macos
    runs-on: ubuntu-latest

    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: Isolated Greenhouse Build ${{ github.run_number }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          files: |
            release-artifacts/IsolatedGreenhouse-Windows/IsolatedGreenhouse-Windows.zip
            release-artifacts/IsolatedGreenhouse-macOS/IsolatedGreenhouse-macOS.zip
